# nvimtool

## Notes

Maybe add support for running/querying nvim with only one plugin, in its default settings
  -> to get default info (keybinds, highlights, etc.)

## Roadmap

- [ ] add treesitter subcommand
- [ ] full test coverage (or at least coverage of the core logic)
